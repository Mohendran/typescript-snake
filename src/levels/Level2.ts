import { Level } from '../types/index';

/**
 * This functon is used to maintain data for the second level
 */

export const LEVEL_DATA: Level = {
    id: 2,
    name: 'Intermediate',
    speed: 200
};
